```
	2021年10月19日14:02:24创建
```

# 目录

[TOC]

# C 语言总结

# Hello World

## 简写表

```
	
	//此处为简写 内容 例子 Hello > H 某些特殊情况下全写
	
```

## C 语言简介


    	广泛 用于底层开发 对硬件层
    	
    	C 丹尼斯 里奇 和 肯 汤普逊 C++几乎是C的超集
    
        C的 设计理念 用户能轻松完成 自顶向下 的 规划 结构化编程 和 模块化设计 用C语言 编写的程序更易懂更可靠 功能强大且灵活 面向程序员(提供了很多自由但是也带来了许多莫名其妙的问题) 高效性 C程序比较紧凑(程序更小) 运行速度快 强大的控制结构 可移植到其他计算机(稍作修改或不修改就能在其它系统运行)
        缺点 C语言的指针错误 自由度很高程序员必须承担更多的责任 代码紧凑代码可能晦涩难懂


### 编程机制

```
	
		常见编程环境(系统) UNIX Linux MS-DOS Windows "Macintosh OS" 
		编写代码的文本文件 成为 源代码文件(source code file)
		大部分系统 要求文件名以.c结尾 //.前称为基本名(basename) 后称为扩展名(extension)	基本名和扩展名组合称为文件名(有些系统限制文件名长度 Linux Win MOS都支持长文件名)
		
```

### 编写工具

```
	
	Win Visual Studio 
	MOS Xcode
	
	VS 配置 https://www.bilibili.com/read/cv15712880
	
```

```
	
	面向对象是一门哲学 能通过对语言建模适应问题 而不是对问题建模适应语言
	
	ANSI/ISO C 标准 信任程序员 不要妨碍程序员做需要做的事 保持语言精炼简单 只提供一种方法执行一项操作 让程序运行更快即使不能保证其可移植性(针对计算机定义最合适的操作 而不是强加一个抽象统一的定义)
	
```

```
		
	使用C语言的七个步骤(理性状态 实际开发可能会做一些重复的调整或改进上一个操作)
	
 	一 定义程序的目标(概念性)(要有清晰的解决问题的思路 思考程序 需要哪些信息 进行哪些计算和控制 程序应该要报告什么 不思考具体怎么写)
    二 设计程序(考虑如何用程序来实现 如何组织程序 目标用户 准备花费多长时间 决定一些数据在程序中如何表示)
    三 编写代码(C语言实现设计的程序)
    四 编译(编译的细节取决于编程的环境 编译器对程序进行检查和编译)
    五 运行程序
    六 测试和调试程序
    七 维护和修改程序
    
    //定义程序目标 设计程序 小程序通常可以被省略增加效率 写庞大复杂的程序时进行定义和设计可以用时间替换效率和稳定性
		
```

### 一些定义

```
	
	.c c语言源文件
	.h 头文件
	
```

## C 的基本代码

```c
	
  
    #include <stdio.h>
	
	int a = 10; //全局变量

    int	main() {
        int a = 10; //局部变量 局部优先
        
   	 	printf("%d", a);
  	    return 0;
    }
	
	//main() 主函数 程序入口 仅有一个
	//int 返回值  也可以void 但是已经过时的
	//printf 库函数 C语言本身 提供给的函数
	//#include 包含stdio.h 文件 导入库文件 standard input output 标准输出输入库
	
```

### 变量定义

```
	
	变量类型 变量名 = 变量值;
	
	全局变量的作用域 是整个工程
	
```

### 数据类型

```c
	
	char //字符数据类型 1B 2的8次方(256)
	short //短整形 2B 2的16次方(65536)
	int //整形 4B
	long //长整形 4/8B
	long long //更长的整形 8B
	float //单精度浮点 4B
	double //双精度浮点 8B
	
	float weight = 95.6; //这样定义 95.6定义为double 转为float 可能定义为精度丢失 
		float weight = 95.6f; //这样定义正确
	//C 语言标准规定 sizeof(long) >= sizeof(int) 即可 所以有时候是4/8

```

### print //没啥实际价值

```c
	
	print("%c\n",变量); //打印字符格式的数据 \n 为换行
	//%d 打印十进制 数据	
	//%c 打印字符
	//%f 打印浮点数字 打印double的时候 %lf
	//%p 以地址的形式打印
	//%x 打印16进制数字

	%某某 算是一个占位符 可以进行拼接
	printf("前面%d后面%d", 2, 3);

```

### scanf //输入函数

```
	
	scanf("%d%d",&变量名,&变量名); //&放入该变量名的地址 输入两个数 也可单个用
	*// &要加
	
```

### 常量

```

	字面常量 
	const修饰的常变量 
	#define定义的标识符常量 
	枚举常量
	
```

#### 字面常量

```
	
	//直接的值
	3; 
	3.14;
	
```

#### const修饰的常变量 

```c
	
	const int num = 4;
	//num不能再改变量 常量的变量
	*//n是 变量但是又有常属性 所以说num 是常变量
	
	const int n = 10;
    int arr[n] = {0};
    //arr[n] 这样是错误的
	
```

#### #define定义的标识符常量 

```c
	
	#define MAX 10 
	int main(){
        int arr[MAX] = {0}; 
    }
	//正确
	//定义在main方法外
	
```

#### 枚举常量

```c
	
	枚举 //一一列举
	
	枚举关键字enum
	
	//定义在main方法外 
    //MALE FEMALE SECRET 叫枚举常量 //分别代表 男 女 保密
	enum Sex
	{
		MALE,
		FEMALE,
		SECRET
	};
	int main(){
        enum Sex s = FEMALE; //输出为1 MALE 0  SECRET2
    }
	*//注意 s 是可以改的 而 枚举常量 是不可以改 而s变量 是可以改变的 性别本身是不可以改变的
	
```

### 字符串

```c
	
	注意字符串 用字符 数组 声明的 一定要在末尾加\0

	"" //双引号引起的 叫做字符串 字符串的末尾后面会有个隐式的\0
        
        //也可
    	char arr1[] = "abc";
		printf("%s", arr1);
		//结果为 abc
		
		char arr2[] = {'a', 'b', 'c'};
		printf("%s", arr2);
		//结果为 abc烫烫烫烫烫烫烫烫烫烫烫烫蘒~g?
	
		*// arr1 数组保存了4个东西 a b c \0(值为0)
		//arr2 没有\0     // \0 代表字符串的 结束标志
            
        char arr3[] = {'a', 'b', 'c', '0'};
		printf("%s", arr3);
		//结果为 abc烫烫烫烫烫烫烫烫烫烫烫烫蘒~g?
		
		char arr4[] = { 'a', 'b', 'c', '\0'};
		printf("%s", arr4);
		//abc
	
	*// 字符串 的结束 标志 是一个\0 的转义字符 在计算机字符串 长度的时候 \0是 结束标志 不算作字符串内容
       
       #include <string.h>
       printf("%d",strlen(arr1))  //计算字符串的长度 3 
       printf("%d",strlen(arr2))  //随机值 直到找到0
       printf("%d",strlen(arr3))  //随机值 直到找到0
       printf("%d",strlen(arr4))  //3 
        
        
       strcpy(拷贝到哪里,拷贝什么) 字符串拷贝 库函数 属于string.h
	
```

### 转义字符

```c

	"\0"	//字符串结尾
	"\n"    //换行
	"\r"	//回车
	"\t"	//水平制表符 
	"\v"	//垂直制表符
	"\\"	//表示普通\
	"\'"	//表示普通'
	"\""	//表示普通"
	"\ddd"  //表示1 ddd表示1-3八进制  
	"\xdd"	//表示2个十六进制数字 
    "\b"	//退格符
	
	printf("%d\n",strlen("c:\test\32\test.c"));
	*//长度为13 \t \32 都是转义字符 算一个数 
	*// \32是 八进制的代表的十进制数的 ASCII码值 
	
```

### 注释

```c
	
	// c++的注释风格
	
	/**/ c的注释风格  *//注意c的注释 不能嵌套 
	
```

### 关键字

```
	
	auto //局部变量 前面都有一个隐式的auto关键字 都有所以省略掉了
	register int a = 10; //建议吧a定义成寄存器变量
	int 定义的时候是有符号的 signed int signed隐式的  //也有unsigned 是无符号位的
	
	typedef 类型定义 -类型重定义
		unsigned int num = 20;
		//这样比较长
		//简化 相当于起别名
		typedef unsigned int u_int;
		u_int num = 20;
		
	static   
		修饰局部变量 static int a = 1; //静态 常量 只执行一次 //局部变量生命周期变长
		修饰全局变量 改变了变量的作用域 让静态的全局变量只能在自己所在的源文件内部使用
		修饰函数 修饰的函数改变了函数的链接属性 普通的函数是有外部链接属性的 外部链接属性 变成了内部链接属性
		修饰静态代码块
		
	extern //声明外部符号的
		//在别的类定义全局变量 int g_val = 2020;
		//另一个类使用 extern int g_val; //此时g_val 可以跨类使用 //也可声明外部函数
		
	#define //类似于定义一些简写
		定义标识符常量 #define MAX 100;
		定义宏带参数 #define MAX(X,Y) (X>Y? X:Y) //使用MAX(a,b) 会被替换为(a>b? a:b)
		
	
```

### 操作符

```c
	
	//c语言用 0表示假 所有非0 代表真

	算数操作符 + - * / %
	
    移位操作符 >> <<
        //二进制位操作
        int a = 1; 00000000000000000000000000000001
        int b = a<<1; 00000000000000000000000000000010 //b=2
		
	位操作符& ^(异或) |
        //二进制位操作
        //异或 二进制位相异 为1
        int a = 3; 00000000000000000000000000000011
        int b = 5; 00000000000000000000000000000101
        int c = a&b; 00000000000000000000000000000001 //c=1
		int c = a|b; 00000000000000000000000000000111 //c=7
        int c = a^b; 00000000000000000000000000000110 //c=6

    赋值操作符 = += -= *= /= &= ^= |= >>= <<=
        除等号外叫 复合操作符
    
    条件操作符 表达式1? 表达式2 : 表达式3
            
    单目操作符
            
        //只有一个操作数  
        !   !10 = 0  !0=1 //! 要放在前
        sizeof //操作数的类型长度 字节单位
            对变量的时候()可省略 对类型不可以省略
            printf("%d", sizeof a); //可以
			printf("%d", sizeof int); //不可以
        ~ //二进制数 按位取反
            0 00000000000000000000000000000000
            ~0 11111111111111111111111111111111(补码) 10000000000000000000000000000001(原码) //-1 负数在内存中储存的时候 是二进制的补码 
            
            
        * //间接访问操作符(解引用操作符)
        (类型) //强制类型转换
        //定义函数时 函数名() //()叫做函数调用操作符
            
    双目操作符
    	//有两个操作数
            
    三目操作符
            
            
            
       & * . ->
	
```

### 选择语句

```
	
	if
	
	else
	
	
```

### 循环语句

```
	
	while(条件表达式){
		
	}
	
```

## C 数组

```
	
	int arr[2] = {0,1};
	//定义一个数组
	//下标从零开始
	
	打印数组
		while实现
		
	
	
```

## C 函数

```
	
	定义方法
	
	返回值类型 方法名称(参数列表){
		方法体
		return 返回值;
	}
	
```

## C 指针

```
	
	每个内存单元的大小是一个字节
	
	有一种变量 是用来存放地址 //指针变量
	
		int a = 10;
		int* p = &a; //指针类型和 指向的类型是一样的 char 为char* //p为指针变量 类型是int* //此处的*没有特别意义只是指明p是个指针变量 而下面的是个操作符
		*p = 20; //*间接/解引用操作符 //a现在为20
		
		*//指针大小在 32位机是4字节 64位机是8字节 可以用来分辨是64位机还是32位机器
	
```

## C 结构体

```
	
	有 书 这样的复杂对象 //自己创造出来一种类型
	
	struct Book{
		char name[20]; //书名
		short price; //55
	};
	
	使用时
	int main(){
		struct Book b1 = {"C语言程序设计",55};
		printf(b1.name); //书名
		printf(b1.price); //价格
		
		//指针
		struct Book* pb = &b1;
		printf((*pb).name); //书名 下面简化
		printf(pb->name); //
	}
	
	. //应用到结构体变量
	-> //结构体指针-> 成员

```



## 实例代码

### 两数相加

```c
	
	#include <stdio.h>
    #pragma warning(disable:4996)
    int	main() {
        int num1 = 0;
        int num2 = 0;
        scanf("%d%d", &num1, &num2);
        int sum = num1 + num2;
        printf("%d", sum);
        return 0;
    }
	
```

### 计算数组的元素个数

```c
	
	#include <stdio.h>
    #pragma warning(disable:4996)
    int	main() {
        int arr[2] = { 1,2 };
		printf("%d", sizeof arr/sizeof (arr[0]));
        return 0;
    }
	
```

## 题

```
	
	C 语言不能 自定义关键字
	define 是个命令 不是关键字
	static 修饰的变量可以被改变
	
```



```
	
	//取消 度娘栏 简化内容 笔记只 记录 重要的纲要结构 自己读懂就好 但是要有良好的纲要结构 保持高度可读性
	/总结的图片 最后是学完之后或者 有大部分内容了之后 凭借感觉选的图
	
```

